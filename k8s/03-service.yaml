# =============================================================================
# compliance-api/03-service.yaml
# =============================================================================
# ClusterIP Service exposing the compliance API within the cluster.
# Traffic flows: Ingress -> Service -> Pod:8000
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: threat-api
  namespace: threat-api
  labels:
    app: threat-api
    app.kubernetes.io/name: threat-api
    app.kubernetes.io/part-of: threat-api
spec:
  type: ClusterIP
  selector:
    app: threat-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
